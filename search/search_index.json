{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"TilekarOS Documentation","text":"<p>Welcome to the TilekarOS documentation. This project is a 32-bit hobby operating system designed for educational purposes.</p>"},{"location":"#documentation-map","title":"Documentation Map","text":""},{"location":"#1-getting-started","title":"1. Getting Started","text":"<ul> <li>Build System: How to compile and run the OS.</li> <li>Installation Guide: Setting up the development environment.</li> <li>Experimenting &amp; Messing Around: How to create tasks and intentionally trigger failures.</li> </ul>"},{"location":"#2-kernel-internals-for-os-developers","title":"2. Kernel Internals (for OS Developers)","text":"<ul> <li>Architecture Overview: Boot sequence and memory layout.</li> <li>Kernel Initialization: Detailed startup sequence from GRUB to <code>kernel_main</code>.</li> <li>GDT &amp; TSS: Global Descriptor Table and segment management.</li> <li>Memory Management: Physical Memory (PMM), Virtual Memory (VMM), and Heap (<code>kmalloc</code>).</li> <li>Interrupts &amp; Exceptions: IDT, PIC, and IRQ handling.</li> <li>Multitasking: Context switching assembly and scheduler logic.</li> <li>Task Management Guide: Creating and managing kernel and user tasks.</li> <li>System Call Dispatcher: How the kernel handles <code>int 0x80</code>.</li> <li>Kernel Utilities: Low-level helpers (I/O ports, interrupt control, <code>InteruptReg</code>).</li> </ul>"},{"location":"#3-drivers","title":"3. Drivers","text":"<ul> <li>VGA/TTY: Text mode output and terminal logic.</li> <li>Keyboard: PS/2 Keyboard and scancode handling.</li> <li>Timer: PIT (Programmable Interval Timer).</li> </ul>"},{"location":"#4-api-reference-for-usersprogrammers","title":"4. API Reference (for Users/Programmers)","text":"<ul> <li>System Call Reference: How to use the kernel services.</li> <li>LibC Reference: Available C standard library functions.</li> </ul>"},{"location":"#5-development-contributions","title":"5. Development &amp; Contributions","text":"<ul> <li>Coding Style: Best practices for writing kernel code.</li> <li>Documentation Guide: How to contribute to this documentation.</li> </ul>"},{"location":"#technical-specs","title":"Technical Specs","text":"<ul> <li>Architecture: i386 (32-bit x86)</li> <li>Bootloader: Multiboot-compliant (e.g., GRUB)</li> <li>Memory Model: Flat / Paged</li> <li>Kernel Space: Higher-half (starts at 0xC0000000)</li> </ul>"},{"location":"api/libc-reference/","title":"LibC Reference","text":"<p>TilekarOS includes a minimal <code>libc</code> implementation for kernel and future user-space use.</p>"},{"location":"api/libc-reference/#1-stdioh-standard-inputoutput","title":"1. stdio.h (Standard Input/Output)","text":""},{"location":"api/libc-reference/#printf","title":"<code>printf</code>","text":"<p><pre><code>int printf(const char* format, ...);\n</code></pre> Supports basic format specifiers: <code>%s</code>, <code>%c</code>, <code>%d</code>, <code>%x</code>.</p>"},{"location":"api/libc-reference/#puts","title":"<code>puts</code>","text":"<p><pre><code>int puts(const char* s);\n</code></pre> Writes a string and a newline to stdout.</p>"},{"location":"api/libc-reference/#putchar","title":"<code>putchar</code>","text":"<p><pre><code>int putchar(int c);\n</code></pre> Writes a single character to stdout.</p>"},{"location":"api/libc-reference/#2-stringh-string-manipulation","title":"2. string.h (String Manipulation)","text":""},{"location":"api/libc-reference/#memcpy","title":"<code>memcpy</code>","text":"<p><pre><code>void* memcpy(void* dest, const void* src, size_t n);\n</code></pre> Copies <code>n</code> bytes from <code>src</code> to <code>dest</code>.</p>"},{"location":"api/libc-reference/#memmove","title":"<code>memmove</code>","text":"<p><pre><code>void* memmove(void* dest, const void* src, size_t n);\n</code></pre> Copies <code>n</code> bytes from <code>src</code> to <code>dest</code>, handling overlapping memory regions safely.</p>"},{"location":"api/libc-reference/#memset","title":"<code>memset</code>","text":"<p><pre><code>void* memset(void* s, int c, size_t n);\n</code></pre> Fills memory with a constant byte.</p>"},{"location":"api/libc-reference/#memcmp","title":"<code>memcmp</code>","text":"<p><pre><code>int memcmp(const void* s1, const void* s2, size_t n);\n</code></pre> Compares the first <code>n</code> bytes of two memory areas.</p>"},{"location":"api/libc-reference/#strlen","title":"<code>strlen</code>","text":"<p><pre><code>size_t strlen(const char* s);\n</code></pre> Calculates the length of a string.</p>"},{"location":"api/libc-reference/#strcmp","title":"<code>strcmp</code>","text":"<p><pre><code>int strcmp(const char* s1, const char* s2);\n</code></pre> Compares two strings.</p>"},{"location":"api/libc-reference/#strcpy","title":"<code>strcpy</code>","text":"<p><pre><code>char* strcpy(char* dest, const char* src);\n</code></pre> Copies a string from <code>src</code> to <code>dest</code>.</p>"},{"location":"api/libc-reference/#3-stdlibh-standard-library","title":"3. stdlib.h (Standard Library)","text":""},{"location":"api/libc-reference/#abort","title":"<code>abort</code>","text":"<p><pre><code>void abort(void);\n</code></pre> Causes abnormal program termination. In the kernel, this typically triggers a panic.</p>"},{"location":"api/syscall-reference/","title":"System Call Reference","text":"<p>This document describes the system call API available for TilekarOS applications.</p>"},{"location":"api/syscall-reference/#1-overview","title":"1. Overview","text":"<p>System calls are accessed via the <code>int 0x80</code> instruction. The syscall number is passed in <code>EAX</code>, and arguments are passed in <code>EBX</code>, <code>ECX</code>, <code>EDX</code>, <code>ESI</code>, and <code>EDI</code>.</p>"},{"location":"api/syscall-reference/#inline-assembly-wrapper-c","title":"Inline Assembly Wrapper (C)","text":"<pre><code>uint32_t syscall(uint32_t num, uint32_t a, uint32_t b, uint32_t c, uint32_t d, uint32_t e) {\n    int ret;\n    asm volatile (\n        \"int $0x80\"\n        : \"=a\"(ret)\n        : \"a\"(num), \"b\"(a), \"c\"(b), \"d\"(c), \"S\"(d), \"D\"(e)\n    );\n    return ret;\n}\n</code></pre>"},{"location":"api/syscall-reference/#2-system-calls","title":"2. System Calls","text":""},{"location":"api/syscall-reference/#sys_exit-0","title":"SYS_EXIT (0)","text":"<p>Terminates the current process.</p> <ul> <li>EAX: 0</li> <li>EBX: Status code (currently ignored)</li> </ul>"},{"location":"api/syscall-reference/#sys_write-1","title":"SYS_WRITE (1)","text":"<p>Writes data to a file descriptor.</p> <ul> <li>EAX: 1</li> <li>EBX: File Descriptor (currently only 1 for stdout)</li> <li>ECX: Buffer Pointer</li> <li>EDX: Length</li> </ul> <p>Returns: Number of bytes written.</p>"},{"location":"api/syscall-reference/#sys_get_pid-2","title":"SYS_GET_PID (2)","text":"<p>Returns the Process ID of the current task.</p> <ul> <li>EAX: 2</li> </ul> <p>Returns: Current task ID.</p>"},{"location":"development/coding-style/","title":"Coding Style &amp; Guidelines","text":"<p>To maintain a clean and consistent codebase, TilekarOS follows these guidelines.</p>"},{"location":"development/coding-style/#1-c-language-style","title":"1. C Language Style","text":"<ul> <li>Indentation: 4 spaces (No tabs).</li> <li>Naming:</li> <li>Functions: <code>snake_case()</code> (e.g., <code>terminal_putchar</code>).</li> <li>Variables: <code>snake_case</code>.</li> <li>Types: <code>snake_case_t</code> (e.g., <code>task_t</code>).</li> <li>Macros/Constants: <code>UPPER_SNAKE_CASE</code>.</li> <li>Braces: K&amp;R style (Brace on the same line for <code>if</code>/<code>while</code>, new line for functions).</li> <li>Headers: Use <code>#ifndef HEADER_NAME_H</code> guards.</li> </ul>"},{"location":"development/coding-style/#2-assembly-style","title":"2. Assembly Style","text":"<ul> <li>NASM: Use Intel syntax.</li> <li>Comments: Use <code>;</code> for comments.</li> <li>Organization: Label global entry points with an underscore (e.g., <code>_start</code>).</li> </ul>"},{"location":"development/coding-style/#3-documentation","title":"3. Documentation","text":"<ul> <li>Every new public function in the kernel should have a brief comment explaining its parameters and return value.</li> <li>Significant changes should be reflected in the relevant file in the <code>docs/</code> directory.</li> </ul>"},{"location":"development/coding-style/#4-git","title":"4. Git","text":"<ul> <li>Keep commits small and focused.</li> <li>Use descriptive commit messages.</li> </ul>"},{"location":"development/documentation-guide/","title":"Documentation Guide","text":"<p>This guide explains how to write and maintain documentation for the TilekarOS project.</p>"},{"location":"development/documentation-guide/#1-structure","title":"1. Structure","text":"<p>Documentation is organized into logical sections:</p> <ul> <li><code>getting-started/</code>: Installation, building, and running.</li> <li><code>kernel/</code>: Core internals (memory, tasks, interrupts).</li> <li><code>drivers/</code>: Hardware-specific implementation details.</li> <li><code>api/</code>: Programmer-facing system calls and LibC reference.</li> <li><code>development/</code>: Coding standards and contribution guides.</li> </ul>"},{"location":"development/documentation-guide/#2-mermaid-diagrams","title":"2. Mermaid Diagrams","text":"<p>TilekarOS uses the default Mermaid theme provided by MkDocs Material. This ensures that diagrams are automatically styled correctly for both light and dark modes without manual configuration.</p>"},{"location":"development/documentation-guide/#best-practices","title":"Best Practices","text":"<ul> <li>Keep it Simple: Only use diagrams where they add significant value.</li> <li>Quotes: Always put labels in quotes to avoid syntax errors with special characters (e.g., <code>Node[\"Label with (Parentheses)\"]</code>).</li> <li>Standard Shapes: Use standard shapes like <code>rect</code>, <code>circle</code>, and <code>subgraph</code>.</li> </ul>"},{"location":"development/documentation-guide/#3-writing-style","title":"3. Writing Style","text":"<ul> <li>Be Concise: Use high-signal language. Avoid fluff.</li> <li>Code Blocks: Always specify the language (e.g., <code>c`,</code>asm<code>, ````mermaid</code>).</li> <li>Headings: Use hierarchical headings (<code>#</code>, <code>##</code>, <code>###</code>).</li> </ul>"},{"location":"development/documentation-guide/#4-previewing-docs","title":"4. Previewing Docs","text":"<p>TilekarOS uses MkDocs with the Material theme.</p> <ol> <li>Install dependencies:    <pre><code>pip install mkdocs-material\n</code></pre></li> <li>Serve the docs locally:    <pre><code>mkdocs serve\n</code></pre></li> <li>Open <code>http://127.0.0.1:8000</code> in your browser.</li> </ol>"},{"location":"drivers/keyboard/","title":"Keyboard Driver","text":"<p>The keyboard driver handles input from a standard PS/2 keyboard.</p>"},{"location":"drivers/keyboard/#1-hardware-interface","title":"1. Hardware Interface","text":"<ul> <li>IRQ: 1 (Interrupt Vector 33).</li> <li>I/O Ports:</li> <li><code>0x60</code>: Data Port (Read scancodes).</li> <li><code>0x64</code>: Command/Status Port.</li> </ul>"},{"location":"drivers/keyboard/#2-scancodes-mapping","title":"2. Scancodes &amp; Mapping","text":"<p>TilekarOS supports PS/2 Scan Code Set 1.</p> <ul> <li>Make Code: Sent when a key is pressed (e.g., 0x1E for 'A').</li> <li>Break Code: Sent when a key is released (Make Code OR 0x80).</li> <li>Extended Scancodes: Start with <code>0xE0</code> (e.g., arrows, <code>RALT</code>, <code>RCTRL</code>).</li> </ul>"},{"location":"drivers/keyboard/#3-state-tracking","title":"3. State Tracking","text":"<p>The driver tracks the state of modifier keys:</p> <ul> <li>Shift (Left/Right)</li> <li>Ctrl (Left/Right)</li> <li>Alt (Left/Right)</li> <li>CapsLock (Toggle)</li> <li>NumLock (Toggle)</li> </ul>"},{"location":"drivers/keyboard/#4-key-events","title":"4. Key Events","text":"<p>The driver generates a <code>KeyEvent</code> structure for every key press or release: <pre><code>typedef struct {\n    enum KeyCode key_code;\n    uint8_t scancode;\n    bool pressed;\n    bool shift_active;\n    bool ctrl_active;\n    bool alt_active;\n    bool caps_lock;\n    bool num_lock;\n    char character;\n} KeyEvent;\n</code></pre></p>"},{"location":"drivers/keyboard/#5-usage","title":"5. Usage","text":""},{"location":"drivers/keyboard/#callback-system","title":"Callback System","text":"<p>Register a custom callback function to handle keyboard events: <pre><code>void my_keyboard_handler(KeyEvent event) {\n    if (event.pressed &amp;&amp; event.key_code == KEY_ESC) {\n        // Handle ESC...\n    }\n}\n\nkeyboard_set_callback(&amp;my_keyboard_handler);\n</code></pre></p>"},{"location":"drivers/keyboard/#debug-mode-f1","title":"Debug Mode (F1)","text":"<p>Pressing F1 toggles Kernel Debug Mode.</p> <ul> <li>When enabled, the kernel logs unknown scancodes and special key presses.</li> <li>It also enables/disables the VGA text cursor.</li> </ul>"},{"location":"drivers/timer/","title":"Timer Driver (PIT)","text":"<p>The Programmable Interval Timer (PIT) is used for timekeeping and preemption.</p>"},{"location":"drivers/timer/#1-hardware-details","title":"1. Hardware Details","text":"<ul> <li>Frequency: 1.193182 MHz.</li> <li>IRQ: 0 (Interrupt Vector 32).</li> <li>Ports: <code>0x40</code> (Channel 0), <code>0x43</code> (Mode/Command Register).</li> </ul>"},{"location":"drivers/timer/#2-implementation","title":"2. Implementation","text":"<ul> <li><code>timer_init(uint32_t frequency)</code>: Configures the PIT to fire interrupts at a specific rate (e.g., 1000Hz = every 1ms).</li> <li><code>onIrq0(InteruptReg *regs)</code>: The interrupt handler called on every tick.</li> <li>Increments a global <code>ticks</code> counter.</li> <li>Iterates through the active Triggers.</li> </ul>"},{"location":"drivers/timer/#3-trigger-system","title":"3. Trigger System","text":"<p>TilekarOS includes a \"Trigger\" system to schedule periodic tasks without complex multitasking overhead.</p>"},{"location":"drivers/timer/#creating-a-trigger","title":"Creating a Trigger","text":"<p><code>insert_triger(tick_mod, func, flags)</code>: Registers a function <code>func</code> to be called every <code>tick_mod</code> ticks.</p> <ul> <li>Flags:</li> <li><code>TIMER_TRIGGER_ACTIVE</code>: The trigger is currently running.</li> <li><code>TIMER_TRIGGER_USE_GLOBAL</code>: Use the absolute system <code>ticks</code> instead of a local counter.</li> </ul>"},{"location":"drivers/timer/#example","title":"Example","text":"<p>The scheduler uses a trigger to preempt tasks every 100ms: <pre><code>scheduler_trigger_index = insert_triger(100, &amp;task_yield, 0);\n</code></pre></p>"},{"location":"drivers/timer/#4-timekeeping","title":"4. Timekeeping","text":"<p>The timer provides basic timekeeping through:</p> <ul> <li><code>get_ticks()</code>: Returns the number of milliseconds since boot.</li> <li><code>set_ticks(uint32_t)</code>: Manually updates the tick counter.</li> </ul>"},{"location":"drivers/vga-tty/","title":"VGA &amp; TTY Driver","text":"<p>TilekarOS uses the standard VGA Text Mode buffer (0xB8000) for output.</p>"},{"location":"drivers/vga-tty/#1-vga-text-mode","title":"1. VGA Text Mode","text":"<ul> <li>Buffer Address: 0xB8000</li> <li>Dimensions: 80 characters wide by 25 characters high.</li> <li>Entry Format: Each character is 2 bytes.</li> <li>Byte 0: ASCII Character.</li> <li>Byte 1: Color Attribute (Foreground/Background).</li> </ul>"},{"location":"drivers/vga-tty/#2-terminal-logic-ttyc","title":"2. Terminal Logic (<code>tty.c</code>)","text":"<p>The terminal driver provides basic text output functions:</p> <ul> <li><code>init_terminal()</code>: Clears the screen, sets the default color, and enables the VGA cursor.</li> <li><code>terminal_putchar(char c)</code>: Prints a character, handling newlines (<code>\\n</code>), backspace (<code>\\b</code>), and scrolling.</li> <li><code>terminal_writestring(const char* data)</code>: Prints a null-terminated string.</li> <li><code>terminal_scroll()</code>: Moves all lines up by one when the bottom of the screen is reached.</li> </ul>"},{"location":"drivers/vga-tty/#3-serial-port-logging-com1","title":"3. Serial Port Logging (COM1)","text":"<p>In addition to the VGA buffer, <code>terminal_putchar</code> also writes every character to the COM1 Serial Port (<code>0x3F8</code>).</p> <ul> <li>Baud Rate: 38400.</li> <li>Config: 8 data bits, no parity, 1 stop bit.</li> <li>Purpose: Allows headless logging and debugging via an external serial monitor (e.g., QEMU <code>-serial stdio</code>).</li> </ul>"},{"location":"drivers/vga-tty/#4-cursor-management","title":"4. Cursor Management","text":"<p>The hardware cursor is controlled using VGA I/O ports (<code>0x3D4</code>, <code>0x3D5</code>).</p> <ul> <li><code>terminal_enable_cursor(start, end)</code>: Shows the cursor and sets its vertical shape.</li> <li><code>terminal_disable_cursor()</code>: Hides the cursor.</li> <li><code>terminal_update_cursor()</code>: Moves the cursor to the current <code>terminal_row</code> and <code>terminal_column</code>.</li> </ul>"},{"location":"drivers/vga-tty/#5-movement-and-editing","title":"5. Movement and Editing","text":"<p>Extended functions are available for interactive text manipulation:</p> <ul> <li><code>terminal_cursor_up/down/left/right()</code>: Move the cursor.</li> <li><code>terminal_delete_char()</code>: Replaces the current character with a space and moves the cursor right.</li> </ul>"},{"location":"drivers/vga-tty/#6-colors","title":"6. Colors","text":"<p>Available colors are defined in <code>vga.h</code>:</p> <ul> <li><code>VGA_COLOR_BLACK</code>, <code>VGA_COLOR_BLUE</code>, <code>VGA_COLOR_LIGHT_GREY</code>, etc.</li> <li>Attributes are combined using <code>(bg &lt;&lt; 4) | fg</code>.</li> </ul>"},{"location":"getting-started/build-system/","title":"Build System Internals &amp; Developer Guide","text":"<p>This document is the definitive guide to the TilekarOS build system. It is designed for developers who need to understand, extend, or debug the compilation process.</p>"},{"location":"getting-started/build-system/#1-philosophy-architecture","title":"1. Philosophy &amp; Architecture","text":"<p>TilekarOS uses CMake (v3.15+) as its primary build system generator. While <code>Makefiles</code> exist in the project, they are thin wrappers (Facades) intended for user convenience (<code>make</code>, <code>make iso</code>), while CMake handles the heavy lifting of dependency graph generation, compiler configuration, and cross-compilation toolchains.</p>"},{"location":"getting-started/build-system/#why-cmake","title":"Why CMake?","text":"<ul> <li>Cross-Platform: Generates Makefiles, Ninja build files, or VS Code / IDE projects seamlessly.</li> <li>Multi-Architecture: Easy switching between <code>i386</code>, <code>x86_64</code>, <code>arm</code>, etc., using toolchain files.</li> <li>Dependency Management: Correctly handles complex chains like C Header \\(\\rightarrow\\) Python Script \\(\\rightarrow\\) ASM Include \\(\\rightarrow\\) ASM Object \\(\\rightarrow\\) Kernel Binary.</li> </ul>"},{"location":"getting-started/build-system/#the-build-pipeline","title":"The Build Pipeline","text":"<p>The build process transforms raw C and Assembly source code into a bootable ISO image.</p> <pre><code>graph TD\n    subgraph Phase1 [\"Phase 1: Configuration\"]\n        CMake[CMake] --&gt;|Reads| Toolchain[\"cmake/toolchains/ARCH.cmake\"]\n        CMake --&gt;|Reads| CM_Root[\"CMakeLists.txt\"]\n        CMake --&gt;|Generates| BuildEnv[\"Build Directory (Makefiles)\"]\n    end\n\n    subgraph Phase2 [\"Phase 2: Pre-Processing\"]\n        Config_H[include/kernel/config.h]\n        Helper[helpers/h2inc.py]\n\n        Helper --&gt;|Parses| Config_H\n        Helper --&gt;|Generates| Config_Inc[build/config.inc]\n    end\n\n    subgraph Phase3 [\"Phase 3: Compilation\"]\n        direction LR\n        Config_Inc -.-&gt;|Included| ASM_Src[\"Assembly Sources\"]\n\n        ASM_Src --&gt;|nasm| ASM_Obj[\"ASM Objects\"]\n        C_Src[\"C Sources\"] --&gt;|clang| C_Obj[\"C Objects\"]\n        LibC_Src[\"LibC Sources\"] --&gt;|clang| LibC_Lib[\"liblibc.a\"]\n    end\n\n    subgraph Phase4 [\"Phase 4: Linking\"]\n        ASM_Obj &amp; C_Obj &amp; LibC_Lib --&gt; Linker[ld.lld]\n        Linker --&gt;|arch/ARCH/linker.ld| Kernel[\"myos.kernel\"]\n    end\n\n    subgraph Phase5 [\"Phase 5: Packaging\"]\n        Kernel --&gt;|cp| ISO_Dir[isodir]\n        GRUB[grub.cfg] --&gt;|cp| ISO_Dir\n        ISO_Dir --&gt;|grub-mkrescue| ISO[\"myos.iso\"]\n    end</code></pre>"},{"location":"getting-started/build-system/#2-directory-structure-organization","title":"2. Directory Structure &amp; Organization","text":"<p>The build system relies on a strict directory hierarchy to locate architecture-specific code.</p> <pre><code>/\n\u251c\u2500\u2500 CMakeLists.txt           # Root configuration\n\u251c\u2500\u2500 Makefile                 # Wrapper for CMake\n\u251c\u2500\u2500 cmake/\n\u2502   \u2514\u2500\u2500 toolchains/\n\u2502       \u251c\u2500\u2500 i386.cmake       # Toolchain for i386-elf\n\u2502       \u2514\u2500\u2500 x86_64.cmake     # (Future) Toolchain for x86_64-elf\n\u251c\u2500\u2500 kernel/\n\u2502   \u251c\u2500\u2500 CMakeLists.txt       # Kernel target definition\n\u2502   \u2514\u2500\u2500 arch/\n\u2502       \u251c\u2500\u2500 i386/            # i386 specific C/ASM code &amp; linker scripts\n\u2502       \u2514\u2500\u2500 x86_64/          # (Future) x86_64 specific code\n\u251c\u2500\u2500 libc/\n\u2502   \u251c\u2500\u2500 CMakeLists.txt       # LibC target definition\n\u2502   \u2514\u2500\u2500 arch/\n\u2502       \u2514\u2500\u2500 i386/            # Architecture-optimized LibC functions\n\u2514\u2500\u2500 sysroot/                 # Virtual filesystem root for cross-compilation\n</code></pre>"},{"location":"getting-started/build-system/#3-multi-architecture-support","title":"3. Multi-Architecture Support","text":"<p>The build system is designed to be Architecture Agnostic. The target architecture is controlled by the <code>OS_ARCH</code> variable.</p>"},{"location":"getting-started/build-system/#how-it-works","title":"How it works","text":"<ol> <li>Selection: The user selects an architecture (default: <code>i386</code>).     <pre><code>make ARCH=i386     # Default\nmake ARCH=x86_64   # (Example)\n</code></pre></li> <li>Toolchain Loading: CMake looks for <code>cmake/toolchains/${OS_ARCH}.cmake</code>. This file sets the compiler (<code>clang</code>), assembler (<code>nasm</code>), and target flags (<code>--target=...</code>).</li> <li>Source Selection:<ul> <li>Kernel: <code>kernel/CMakeLists.txt</code> globs files from <code>kernel/arch/${OS_ARCH}/*</code>.</li> <li>LibC: <code>libc/CMakeLists.txt</code> globs files from <code>libc/arch/${OS_ARCH}/*</code>.</li> </ul> </li> <li>Emulation: The <code>run</code> targets dynamically choose the emulator (e.g., <code>qemu-system-${OS_ARCH}</code>).</li> </ol>"},{"location":"getting-started/build-system/#adding-a-new-architecture","title":"Adding a New Architecture","text":"<p>To port TilekarOS to a new architecture (e.g., <code>riscv64</code>):</p> <ol> <li>Create Toolchain: Add <code>cmake/toolchains/riscv64.cmake</code>.     <pre><code>set(CMAKE_SYSTEM_PROCESSOR riscv64)\nset(CMAKE_C_COMPILER_TARGET riscv64-unknown-elf)\n# ... set CMAKE_ASM_COMPILER ...\n</code></pre></li> <li>Create Kernel Arch Directory: Create <code>kernel/arch/riscv64/</code>.<ul> <li>Add <code>boot.asm</code> / <code>entry.S</code>.</li> <li>Add <code>linker.ld</code> (Linker script).</li> <li>Add <code>local_config.h</code>.</li> </ul> </li> <li>Create LibC Arch Directory: Create <code>libc/arch/riscv64/</code>.<ul> <li>Add architecture-specific optimizations (or leave empty if generic C is fine).</li> </ul> </li> <li>Build: Run <code>make ARCH=riscv64</code>.</li> </ol>"},{"location":"getting-started/build-system/#4-helper-scripts-custom-commands","title":"4. Helper Scripts &amp; Custom Commands","text":""},{"location":"getting-started/build-system/#helpersh2incpy-the-bridge","title":"<code>helpers/h2inc.py</code> (The Bridge)","text":"<p>Problem: We define constants like <code>GDT_OFFSET_KERNEL_CODE = 0x08</code> in C header files (<code>config.h</code>). The assembly code (<code>boot.asm</code>) needs these exact values to set up segments. Hardcoding them in two places leads to \"Magic Number\" bugs.</p> <p>Solution:</p> <ol> <li>CMake invokes <code>h2inc.py</code> during the build.</li> <li>The script reads C <code>#define</code> macros.</li> <li>It outputs a NASM-compatible <code>%define</code> file (<code>build/config.inc</code>).</li> <li>NASM files include this generated file via the <code>-P</code> flag.</li> </ol> <p>Usage in CMake: <pre><code>add_custom_command(\n    OUTPUT ${CONFIG_INC}\n    COMMAND Python3::Interpreter \"${CMAKE_SOURCE_DIR}/helpers/h2inc.py\" ${CONFIG_H} ${CONFIG_INC}\n    ...\n)\n</code></pre></p>"},{"location":"getting-started/build-system/#5-ide-integration-clangd","title":"5. IDE Integration (.clangd)","text":"<p>To ensure a smooth development experience in editors like VS Code or Zed (which use <code>clangd</code>), the root <code>CMakeLists.txt</code> generates a <code>.clangd</code> file.</p> <p>This file tells the language server: *   We are cross-compiling (e.g., <code>--target=i386-elf</code>). *   Where to find the kernel headers (<code>-Ikernel/include</code>). *   Where to find the system headers (<code>-Isysroot/usr/include</code>).</p> <p>Result: You get accurate Autocomplete and Error Checking for the target OS, not the host Linux system.</p>"},{"location":"getting-started/build-system/#6-command-reference","title":"6. Command Reference","text":"Command Variables Description <code>make</code> <code>ARCH=...</code> Builds the kernel binary. <code>make iso</code> <code>ARCH=...</code> Builds the bootable ISO image. <code>make run</code> <code>ARCH=...</code> Runs the kernel binary directly in QEMU. <code>make run_iso</code> <code>ARCH=...</code> Boots the ISO in QEMU (Tests GRUB integration). <code>make clean</code> Removes the <code>build/</code> directory. <code>make configure</code> <code>ARCH=...</code> Re-runs the CMake configuration step manually. <p>Example: <pre><code># Build and run the ISO for i386\nmake run_iso\n\n# Build for x86_64 (assuming files exist)\nmake ARCH=x86_64\n</code></pre></p>"},{"location":"getting-started/experimenting/","title":"Experimenting with TilekarOS","text":"<p>TilekarOS is designed to be a playground for learning OS internals. This guide shows you how to \"mess\" with the system, create tasks, and intentionally trigger failures to see how the kernel responds.</p>"},{"location":"getting-started/experimenting/#1-creating-kernel-tasks-ring-0","title":"1. Creating Kernel Tasks (Ring 0)","text":"<p>Kernel tasks run with full privileges. They are easy to create and can access any part of the hardware.</p>"},{"location":"getting-started/experimenting/#how-to-add-a-task","title":"How to add a task:","text":"<ol> <li>Define a function in <code>kernel/kernel.c</code>:     <pre><code>void my_experiment_task() {\n    while (true) {\n        printf(\"[Experiment] Running...\\n\");\n        for (volatile int i = 0; i &lt; 1000000; i++); // Busy wait\n        task_yield(NULL); // Give up CPU\n    }\n}\n</code></pre></li> <li>Register it in <code>kernel_main()</code>:     <pre><code>task_create(my_experiment_task, 0);\n</code></pre></li> </ol>"},{"location":"getting-started/experimenting/#voluntary-vs-preemptive","title":"Voluntary vs. Preemptive:","text":"<ul> <li><code>task_yield(NULL)</code>: Manually tells the scheduler to pick the next task.</li> <li>Preemption: If you don't call yield, the Timer Interrupt will eventually force a context switch after 10ms.</li> </ul>"},{"location":"getting-started/experimenting/#2-creating-user-tasks-ring-3","title":"2. Creating User Tasks (Ring 3)","text":"<p>User tasks are isolated and run in their own virtual address space. They cannot access kernel memory or execute privileged instructions.</p>"},{"location":"getting-started/experimenting/#the-lifecycle-of-a-user-task","title":"The Lifecycle of a User Task","text":"<p>In TilekarOS, a user task is typically defined as a block of code (assembly or C) that is copied into a new address space.</p>"},{"location":"getting-started/experimenting/#step-1-define-the-user-code","title":"Step 1: Define the User Code","text":"<p>User tasks use System Calls to interact with the kernel. For example, to print \"Hello from Ring 3\" and exit:</p> <pre><code>[bits 32]\n\nsection .text\n\nglobal _start_user_task\nglobal _end_user_task\n\n_start_user_task:\n    ; SYS_WRITE (1), STDOUT (1)\n    mov eax, 1\n    mov ebx, 1\n    mov ecx, msg\n    mov edx, 19\n    int 0x80\n\n    ; SYS_EXIT (0)\n    mov eax, 0\n    xor ebx, ebx\n    int 0x80\n\nmsg db 'Hello from Ring 3!', 0xA, 0\n_end_user_task:\n</code></pre>"},{"location":"getting-started/experimenting/#step-2-launch-the-task","title":"Step 2: Launch the Task","text":"<p>In <code>kernel_main</code>, use the <code>extern</code> symbols to pass the code block to <code>task_create_user</code>: <pre><code>extern char _start_user_task;\nextern char _end_user_task;\n\ntask_create_user(&amp;_start_user_task, &amp;_end_user_task);\n</code></pre></p>"},{"location":"getting-started/experimenting/#what-happens-under-the-hood","title":"What happens under the hood?","text":"<ol> <li>Memory Mapping: The code block is copied to a new page directory starting at <code>0x08048000</code>.</li> <li>Stack Setup: A user-mode stack is automatically mapped at <code>0xB0000000</code>.</li> <li>Ring Transition: The kernel uses the <code>iret</code> instruction to drop the CPU's privilege level to Ring 3.</li> <li>TSS Update: The Task State Segment (TSS) is updated with the task's kernel stack pointer (<code>esp0</code>) so the CPU can transition back to Ring 0 during interrupts or syscalls.</li> </ol>"},{"location":"getting-started/experimenting/#technical-deep-dive-the-why-behind-the-implementation","title":"Technical Deep Dive: The \"Why\" behind the implementation","text":"<p>You might wonder why we use specific addresses or why we copy code around. Here is the technical rationale:</p> <ul> <li>Why <code>extern char</code>?: Since TilekarOS doesn't have a filesystem yet, we \"embed\" user programs inside the kernel binary. These symbols tell the kernel exactly where that embedded code starts and ends.</li> <li>Why Copying?: The CPU prevents Ring 3 (User Mode) from accessing any memory marked as \"Supervisor.\" Because the kernel is mapped at <code>0xC0000000</code>, the user task cannot \"reach\" its own code if it stays in the kernel region. We must copy it to a User-accessible page.</li> <li>Why <code>0x08048000</code>?: This is the standard base address for 32-bit x86 executables (ELF). Using it maintains compatibility with traditional toolchains.</li> <li>Why <code>0xB0000000</code>?: We want the stack to be as far away from the code as possible to allow both to grow. This address is high in the user's 3GB space but leaves enough room before hitting the 3GB Kernel boundary.</li> <li>Why <code>iret</code>?: The CPU hardware forbids jumping from Ring 0 to Ring 3. The only way to \"drop\" privilege is to simulate returning from an interrupt. We push a \"fake\" user-mode state onto the stack and execute <code>iret</code>, which tricks the CPU into switching to Ring 3.</li> </ul>"},{"location":"getting-started/experimenting/#4-ring-3-pitfalls-restrictions","title":"4. Ring 3 Pitfalls &amp; Restrictions","text":"<p>When experimenting in Ring 3, remember these rules:</p> <ul> <li>No Direct I/O: <code>outb</code>/<code>inb</code> will trigger a General Protection Fault (GPF).</li> <li>No Privileged Instructions: <code>hlt</code>, <code>cli</code>, <code>sti</code>, <code>lgdt</code>, <code>lidt</code>, <code>mov crX, reg</code> will all cause a GPF.</li> <li>No Kernel Memory: Accessing any address above <code>0xC0000000</code> will trigger a Page Fault.</li> <li>Strictly Syscalls: You must use <code>int 0x80</code> to request services from the kernel.</li> </ul>"},{"location":"getting-started/experimenting/#3-how-to-make-things-fail","title":"3. How to Make Things Fail","text":"<p>Testing the kernel's robustness is part of the fun. Here is how you can trigger common CPU exceptions.</p>"},{"location":"getting-started/experimenting/#a-division-by-zero-isr-0","title":"A. Division by Zero (ISR 0)","text":"<p>Add this to any task to see the \"Division By Zero\" exception message: <pre><code>volatile int a = 5;\nvolatile int b = 0;\nvolatile int c = a / b;\n</code></pre></p>"},{"location":"getting-started/experimenting/#b-page-fault-isr-14","title":"B. Page Fault (ISR 14)","text":"<p>Triggered when a task tries to access memory it doesn't own. - From Kernel: <code>*(int*)0x0 = 123;</code> (Accessing NULL). - From User: Trying to read kernel memory (addresses above <code>0xC0000000</code>).</p>"},{"location":"getting-started/experimenting/#c-general-protection-fault-isr-13","title":"C. General Protection Fault (ISR 13)","text":"<p>Triggered in User Mode if you try to execute privileged instructions: <pre><code>// Inside a User Task\nasm volatile(\"hlt\"); // Only allowed in Ring 0\n</code></pre> The kernel will catch this and usually terminate the offending user task.</p>"},{"location":"getting-started/experimenting/#d-out-of-memory-oom","title":"D. Out of Memory (OOM)","text":"<p>Stress the heap allocator by requesting more memory than available: <pre><code>for (int i = 0; i &lt; 100; i++) {\n    void* p = kmalloc(1024 * 1024); // Request 1MB repeatedly\n    if (!p) printf(\"Heap exhausted at %d MB\\n\", i);\n}\n</code></pre></p>"},{"location":"getting-started/experimenting/#4-debugging-tips","title":"4. Debugging Tips","text":""},{"location":"getting-started/experimenting/#headless-logging-serial-com1","title":"Headless Logging (Serial COM1)","text":"<p>TilekarOS mirrors all <code>printf</code> output to the serial port <code>0x3F8</code>. If you are using QEMU, you can see these logs in your terminal by adding: <pre><code>qemu-system-i386 -cdrom TilekarOS.iso -serial stdio\n</code></pre></p>"},{"location":"getting-started/experimenting/#stack-overflows","title":"Stack Overflows","text":"<p>The kernel allocates 4KB for each task's stack. If you define a massive local array (e.g., <code>char buf[5000];</code>), you will overwrite other kernel data or the <code>task_t</code> structure, leading to a spectacular (and educational) crash.</p>"},{"location":"getting-started/installation/","title":"Installation &amp; Setup","text":"<p>To develop TilekarOS, you need a cross-compilation environment for i386.</p>"},{"location":"getting-started/installation/#1-prerequisites-ubuntudebian","title":"1. Prerequisites (Ubuntu/Debian)","text":"<p>Install the necessary build tools and emulators:</p> <pre><code>sudo apt update\nsudo apt install build-essential cmake nasm qemu-system-x86 grub-pc-bin xorriso mtools python3 clang lld\n</code></pre>"},{"location":"getting-started/installation/#2-recommended-ide","title":"2. Recommended IDE","text":"<p>We recommend using VS Code or Zed with the following extensions: - Clangd: For C/C++ intellisense and cross-compilation support. - ASM-LSP: For NASM syntax highlighting and completions.</p> <p>The project root contains a <code>compile_flags.txt</code> and <code>.clangd</code> file which are automatically generated to help your editor understand the kernel headers.</p>"},{"location":"getting-started/installation/#3-cloning-the-repository","title":"3. Cloning the Repository","text":"<pre><code>git clone https://github.com/your-repo/TilekarOS.git\ncd TilekarOS\n</code></pre>"},{"location":"getting-started/installation/#4-first-build","title":"4. First Build","text":"<pre><code>make configure\nmake iso\nmake run_iso\n</code></pre>"},{"location":"kernel/architecture/","title":"Architecture &amp; Internals","text":"<p>This document provides a deep dive into the internal architecture of TilekarOS. It covers the boot sequence, memory organization, and the interrupt handling mechanism, supported by detailed diagrams.</p>"},{"location":"kernel/architecture/#1-the-boot-sequence","title":"1. The Boot Sequence","text":"<p>The transition from a powered-off machine to the executing kernel involves several stages. TilekarOS relies on a Multiboot-compliant bootloader (like GRUB) to handle the initial hardware setup.</p>"},{"location":"kernel/architecture/#detailed-boot-flowchart","title":"Detailed Boot Flowchart","text":"<pre><code>flowchart TD\n    %% --- Hardware / Bootloader ---\n    subgraph SystemStart [\"System Start\"]\n        direction TB\n        BIOS[\"BIOS / UEFI\"]:::hardware --&gt; GRUB[GRUB Bootloader]:::hardware\n        GRUB -- Multiboot Magic --&gt; ProtMode[\"32-bit Protected Mode\"]:::hardware\n    end\n\n    %% --- Low Level Entry ---\n    subgraph BootASM [\"boot.asm _start\"]\n        direction TB\n        Stack[\"Setup Stack (esp = stack_top)\"]:::asm\n        Stack --&gt; InitHub{Init Sequence}:::asm\n    end\n\n    SystemStart --&gt; BootASM\n\n    %% --- Modules ---\n\n    %% TTY\n    InitHub -- 1 --&gt; TTY[\"tty.c: init_terminal\"]:::c_module\n\n    %% GDT\n    subgraph GDT_Scope [\"gdt.c: init_gdt\"]\n        direction LR\n        G_Ld[Load GDTR]:::c_func --&gt; G_TSS[Install TSS]:::c_func\n        G_TSS --&gt; G_TR[Load TR]:::c_func\n    end\n    InitHub -- 2 --&gt; GDT_Scope\n\n    %% IDT\n    subgraph IDT_Scope [\"idt.c: init_idt\"]\n        direction LR\n        I_PIC[\"Remap PIC (0x20 / 0x28)\"]:::c_func --&gt; I_Fill[Fill Gates]:::c_func\n        I_Fill --&gt; I_Ld[Load IDTR]:::c_func\n    end\n    InitHub -- 3 --&gt; IDT_Scope\n\n    %% Kernel Main\n    subgraph Kernel_Scope [\"kernel.c: kernel_main\"]\n        direction TB\n        AppLogic[\"Print Banner &amp; Run Tests\"]:::kernel --&gt; Halt((Infinite Loop)):::hardware\n    end\n    InitHub -- 4 --&gt; Kernel_Scope\n\n    %% FORCE LEFT-TO-RIGHT ORDERING\n    TTY ~~~ G_Ld ~~~ I_PIC ~~~ AppLogic</code></pre>"},{"location":"kernel/architecture/#steps-explained","title":"Steps Explained","text":"<ol> <li>Bootloader Handoff: GRUB ensures the CPU is in Protected Mode (A20 line enabled, Paging disabled). It places the Multiboot Information Structure in memory and puts the \"Magic Number\" in <code>EAX</code>.</li> <li>Stack Setup: Since C functions require a stack, <code>boot.asm</code> points the Stack Pointer (<code>ESP</code>) to the top of a reserved 16KiB block in the <code>.bss</code> section.</li> <li>Global Descriptor Table (GDT): We replace the GDT provided by GRUB with our own to ensure we have full control over the memory segments and to set up the TSS (Task State Segment) for future ktask-mode switching. See Global Descriptor Table.</li> <li>Interrupt Descriptor Table (IDT): We configure the IDT to handle exceptions (like Divide-by-Zero) and hardware interrupts. The 8259 PIC is remapped to avoid IDT collisions. See Interrupt Descriptor Table.</li> </ol>"},{"location":"kernel/architecture/#2-memory-organization","title":"2. Memory Organization","text":"<p>TilekarOS uses a Flat Memory Model. Paging is currently disabled (Linear Address = Physical Address). See Memory Map (x86).</p>"},{"location":"kernel/architecture/#memory-layout-diagram","title":"Memory Layout Diagram","text":"<pre><code>block-beta\n    columns 1\n\n    block:high_mem\n        space\n        text[\"High Memory (Available for Allocation)\"]\n        space\n    end\n\n    block:kernel_space\n        block:sections\n            columns 3\n            stack[\"Kernel Stack&lt;br&gt;(16 KB)\"]\n            bss[\".bss Section&lt;br&gt;(Uninitialized)\"]\n            data[\".data Section&lt;br&gt;(Initialized)\"]\n        end\n        code[\".text Section&lt;br&gt;(Kernel Code)\"]\n    end\n\n    block:reserved\n        vga[\"VGA Video Memory&lt;br&gt;(0xB8000 - 0xBFFFF)\"]\n        bios[\"BIOS Data Area &amp; EBDA&lt;br&gt;(Reserved)\"]\n    end</code></pre>"},{"location":"kernel/architecture/#gdt-entry-structure","title":"GDT Entry Structure","text":"<p>The GDT defines the characteristics of the various memory segments.</p> <pre><code>packet-beta\n    0-15: \"Limit (0-15)\"\n    16-31: \"Base (0-15)\"\n    32-39: \"Base (16-23)\"\n    40-47: \"Access Byte\"\n    48-51: \"Limit (16-19)\"\n    52-55: \"Flags\"\n    56-63: \"Base (24-31)\"</code></pre> <ul> <li>Base: The starting linear address of the segment (0x0 for Flat Model).</li> <li>Limit: The size of the segment.</li> <li>Access Byte: Defines presence, privilege level (Ring 0 vs 3), and type (Code vs Data).</li> <li>Flags: Granularity (4KiB blocks vs 1 Byte) and Operand size (32-bit vs 16-bit).</li> </ul>"},{"location":"kernel/architecture/#3-interrupt-handling-system","title":"3. Interrupt Handling System","text":"<p>The interrupt system allows the CPU to pause current execution to handle urgent events (Exceptions, Hardware Signals, Syscalls).</p>"},{"location":"kernel/architecture/#interrupt-dispatch-pipeline","title":"Interrupt Dispatch Pipeline","text":"<p>When an interrupt (e.g., IRQ 1 - Keyboard) occurs:</p> <pre><code>sequenceDiagram\n    participant HW as Hardware (Keyboard)\n    participant CPU\n    participant IDT as IDT (Table)\n    participant ASM as ASM Wrapper (isr_common)\n    participant C as C Handler (irq_handler)\n\n    HW-&gt;&gt;CPU: Signals Interrupt (IRQ 1)\n    Note over CPU: 1. Push EFLAGS, CS, EIP&lt;br&gt;2. Read IDT Entry 33\n    CPU-&gt;&gt;ASM: Jump to ISR Stub (irq1)\n    activate ASM\n    ASM-&gt;&gt;ASM: Push Interrupt Number (33)\n    ASM-&gt;&gt;ASM: PushAll (EAX, ECX, EDX...)\n    ASM-&gt;&gt;ASM: Load Kernel Data Segment\n    ASM-&gt;&gt;C: Call irq_handler(registers)\n    activate C\n    C-&gt;&gt;C: Look up registered handler\n    C--&gt;&gt;C: Handle Event (Read Scancode)\n    C-&gt;&gt;HW: Send EOI (End of Interrupt)\n    C-&gt;&gt;ASM: Return\n    deactivate C\n    ASM-&gt;&gt;ASM: Restore Registers (PopAll)\n    ASM-&gt;&gt;CPU: IRET (Interrupt Return)\n    deactivate ASM\n    CPU-&gt;&gt;HW: Resume Execution</code></pre>"},{"location":"kernel/architecture/#idt-configuration","title":"IDT Configuration","text":"<p>The IDT is an array of 256 8-byte entries.</p> Vector Range Usage Description 0 - 31 CPU Exceptions Faults, Traps, and Aborts (e.g., Page Fault, GPF). 32 - 47 Hardware IRQs Remapped PIC interrupts. IRQ0=32 (Timer), IRQ1=33 (Keyboard). 48 - 127 Reserved Available for custom use. 128 (0x80) Syscall Typical Linux-style system call entry point. 177 Syscall Alternate system call entry point."},{"location":"kernel/architecture/#the-pic-remapping-issue","title":"The PIC Remapping Issue","text":"<p>By default, the 8259 PIC maps IRQs 0-7 to Interrupt Vectors 0-7. This conflicts with CPU Exceptions (e.g., INT 0 is Divide-by-Zero). Solution: We reprogram the PIC to offset IRQs to start at vector 32 (0x20).</p> <ul> <li>Master PIC (IRQs 0-7) \\(\\rightarrow\\) Vectors 32-39</li> <li>Slave PIC (IRQs 8-15) \\(\\rightarrow\\) Vectors 40-47</li> </ul>"},{"location":"kernel/architecture/#4-kernel-execution-testing","title":"4. Kernel Execution &amp; Testing","text":"<p>The <code>kernel_main</code> function serves as the entry point for the OS after initialization. Currently, it performs the following actions:</p> <ol> <li>Banner Display: Prints the OS welcome message using <code>terminal_writestring</code>.</li> <li>Exception Test: Intentionally triggers a Divide-by-Zero exception (<code>1/0</code>).<ul> <li>Purpose: To verify that the IDT is correctly loaded and the ISR for Vector 0 is firing.</li> <li>Expected Behavior: The OS should catch the exception, print a debug message (if implemented), and halt or panic, rather than triple-faulting or resetting.</li> </ul> </li> </ol>"},{"location":"kernel/gdt/","title":"Global Descriptor Table (GDT) &amp; Task State Segment (TSS)","text":"<p>The GDT is a fundamental x86 structure that defines memory segments and their access rights. TilekarOS uses a Flat Memory Model, where segments cover the entire 4GB address space.</p>"},{"location":"kernel/gdt/#1-segment-descriptors","title":"1. Segment Descriptors","text":"<p>TilekarOS defines five main descriptors in the GDT:</p> Index Offset Description Base Limit Privilege 0 0x00 Null - - - 1 0x08 Kernel Code 0 4GB Ring 0 2 0x10 Kernel Data 0 4GB Ring 0 3 0x18 User Code 0 4GB Ring 3 4 0x20 User Data 0 4GB Ring 3 5 0x28 TSS &amp;tss size Ring 3"},{"location":"kernel/gdt/#segment-format","title":"Segment Format","text":"<p>Each GDT entry is 8 bytes and includes the Base Address (32-bit), Segment Limit (20-bit), and control bits:</p> <ul> <li>Access Byte: <ul> <li>P (Present): Must be 1.</li> <li>DPL (Privilege): 00 for Kernel, 11 for User.</li> <li>Type: Code (0x18) or Data (0x10).</li> </ul> </li> <li>Flags:<ul> <li>G (Granularity): 1 for 4KB blocks (allows 4GB limit).</li> <li>D (Size): 1 for 32-bit protected mode.</li> </ul> </li> </ul>"},{"location":"kernel/gdt/#2-task-state-segment-tss","title":"2. Task State Segment (TSS)","text":"<p>While TilekarOS uses software-based multitasking, it still requires one hardware TSS to handle privilege transitions (e.g., from User Mode to Kernel Mode).</p>"},{"location":"kernel/gdt/#why-do-we-need-it","title":"Why do we need it?","text":"<p>When an interrupt occurs while the CPU is in Ring 3, it needs to switch to a safe Ring 0 stack. The CPU looks up the <code>esp0</code> field in the TSS to find the kernel stack pointer for the current task.</p>"},{"location":"kernel/gdt/#implementation","title":"Implementation","text":"<ul> <li><code>TSSEntry</code> Structure: Contains pointers for all privilege levels (<code>esp0</code>, <code>esp1</code>, <code>esp2</code>) and current registers. TilekarOS primarily uses the <code>esp0</code> and <code>ss0</code> fields.</li> <li><code>tss_entry</code>: A global structure containing the <code>esp0</code> and <code>ss0</code> values.</li> <li><code>tss_set_kernel_stack(uint32_t stack_base)</code>: Updates <code>esp0</code> during every context switch so that the CPU always uses the correct kernel stack for the active task.</li> </ul>"},{"location":"kernel/gdt/#3-loading-the-gdt","title":"3. Loading the GDT","text":"<p>The GDT is loaded into the CPU using the <code>lgdt</code> instruction. After loading, the segment registers (<code>cs</code>, <code>ds</code>, <code>es</code>, <code>fs</code>, <code>gs</code>, <code>ss</code>) must be reloaded to point to the new descriptors.</p> <pre><code>void init_gdt() {\n  gdt_load_register(&amp;gdt_discriptor);\n  gdt_install_tss();\n  tss_load_register();\n}\n</code></pre>"},{"location":"kernel/interrupts/","title":"Interrupts &amp; Exceptions","text":"<p>The Interrupt system is the backbone of TilekarOS, handling hardware events, CPU faults, and system calls.</p>"},{"location":"kernel/interrupts/#1-the-idt-interrupt-descriptor-table","title":"1. The IDT (Interrupt Descriptor Table)","text":"<p>The IDT is an array of 256 gates. Each gate points to an assembly \"stub\" that handles a specific interrupt.</p>"},{"location":"kernel/interrupts/#idt-structures","title":"IDT Structures","text":"<ul> <li><code>IDTEntry</code>: Each entry is 8 bytes. It contains the 32-bit address of the ISR (split into high/low words), the kernel code segment selector (<code>0x08</code>), and attributes.</li> <li>Attributes: Bits 0-3 (Gate Type), Bit 4 (S), Bits 5-6 (DPL), Bit 7 (P).</li> <li><code>IDTR</code>: A 6-byte structure used by the <code>lidt</code> instruction, containing the limit (size - 1) and the linear address of the IDT array.</li> </ul>"},{"location":"kernel/interrupts/#vector-layout","title":"Vector Layout","text":"<ul> <li>0 - 31: CPU Exceptions (Faults/Traps).</li> <li>0: Divide-by-Zero</li> <li>13: General Protection Fault</li> <li>14: Page Fault (presents faulting address in <code>CR2</code>).</li> <li>32 - 47: Hardware Interrupts (IRQs).</li> <li>32: Timer (PIT)</li> <li>33: Keyboard</li> <li>128 (0x80): System Call Entry.</li> </ul>"},{"location":"kernel/interrupts/#2-programmable-interrupt-controller-pic","title":"2. Programmable Interrupt Controller (PIC)","text":"<p>TilekarOS remaps the 8259 PIC so that IRQs start at index 32, avoiding conflicts with CPU exceptions.</p>"},{"location":"kernel/interrupts/#pic-mapping","title":"PIC Mapping","text":"<ul> <li>Master PIC: Remapped to 0x20 (32). Handles IRQs 0-7.</li> <li>Slave PIC: Remapped to 0x28 (40). Handles IRQs 8-15.</li> </ul>"},{"location":"kernel/interrupts/#end-of-interrupt-eoi","title":"End of Interrupt (EOI)","text":"<p>After handling an IRQ, the kernel MUST send an EOI signal (0x20) to the PIC.</p> <ul> <li>If the IRQ was on the Slave PIC (&gt;= 40), an EOI must be sent to both the Slave and the Master.</li> <li>If the IRQ was only on the Master PIC, an EOI is only sent to the Master.</li> </ul>"},{"location":"kernel/interrupts/#3-interrupt-lifecycle","title":"3. Interrupt Lifecycle","text":"<ol> <li>Hardware/CPU Triggers: CPU pushes <code>EFLAGS</code>, <code>CS</code>, and <code>EIP</code>. If a privilege switch occurred, <code>SS</code> and <code>ESP</code> are also pushed.</li> <li>Assembly Stub (<code>idt.asm</code>):<ul> <li>Pushes an error code (or 0 if none).</li> <li>Pushes the interrupt number.</li> <li>Saves all registers (<code>pushad</code>).</li> <li>Saves segment registers (<code>gs</code>, <code>fs</code>, <code>es</code>, <code>ds</code>).</li> <li>Sets <code>DS</code> and <code>ES</code> to the kernel data segment.</li> </ul> </li> <li>C Handler: <code>irq_handler</code> or <code>isr_handler</code> is called.<ul> <li>All registers are accessible via the <code>InteruptReg</code> pointer.</li> </ul> </li> <li>Restore: Registers are restored (<code>popad</code>), and <code>iret</code> returns to the previous execution flow.</li> </ol>"},{"location":"kernel/interrupts/#4-handler-registration","title":"4. Handler Registration","text":"<p>Kernel modules can register handlers for specific IRQs using <code>irq_install_handler(irq_no, handler)</code>.</p>"},{"location":"kernel/interrupts/#5-register-context-interuptreg","title":"5. Register Context (<code>InteruptReg</code>)","text":"<p>All handlers receive a pointer to the saved registers on the stack: <pre><code>typedef struct {\n    uint32_t gs, fs, es, ds;\n    uint32_t edi, esi, ebp, esp, ebx, edx, ecx, eax;\n    uint32_t intr_num, err_cod;\n    uint32_t eip, csm, eflags, useresp, ss;\n} InteruptReg;\n</code></pre></p>"},{"location":"kernel/kernel-init/","title":"Kernel Initialization Sequence","text":"<p>This document provides a concise overview of the TilekarOS startup process.</p>"},{"location":"kernel/kernel-init/#1-bootloader-phase-grub","title":"1. Bootloader Phase (GRUB)","text":"<ul> <li>GRUB loads the kernel into physical memory at the 1MB mark (0x100000).</li> <li>It enables Protected Mode (32-bit) and the A20 line.</li> <li>It passes the Multiboot Information Structure (memory map, modules, etc.).</li> </ul>"},{"location":"kernel/kernel-init/#multiboot-header","title":"Multiboot Header","text":"<p>Located within the first 8KB of the kernel image, this header is parsed by GRUB: - Magic Number: <code>0x1BADB002</code>. - Flags:      - Bit 0: Page-aligned modules.     - Bit 1: Memory information (e.g., memory map from BIOS). - Checksum: <code>-(magic + flags)</code>.</p>"},{"location":"kernel/kernel-init/#2-low-level-entry-bootasm","title":"2. Low-Level Entry (<code>boot.asm</code>)","text":"<ul> <li>Stack Setup: Point <code>ESP</code> to a reserved 16KB block in the <code>.bss</code> section. The stack is 16-byte aligned.</li> <li>Initial Paging: To bridge the gap before the higher-half kernel is ready, <code>boot.asm</code> sets up a temporary page directory (<code>initial_page_dir</code>):<ul> <li>Identity Mapping: Maps physical 0-4MB to virtual 0-4MB using a 4MB Large Page.</li> <li>Higher-Half Mapping: Maps physical 0-4MB to virtual <code>0xC0000000</code> (3GB).</li> </ul> </li> <li>PSE (Page Size Extensions): Bit 4 of CR4 is set to enable 4MB pages.</li> <li>Enabling Paging: Bit 31 of CR0 is set to enable the paging hardware.</li> <li>Call <code>init_kernel</code>: Control is passed to the C kernel via a long jump to <code>higher_half_kernel</code>.</li> </ul>"},{"location":"kernel/kernel-init/#3-linker-layout-linkerld","title":"3. Linker Layout (<code>linker.ld</code>)","text":"<p>The linker script organizes the kernel binary into several key sections:</p> <ul> <li><code>.multiboot</code>: Placed at the very beginning for GRUB detection.</li> <li><code>.boot</code>: Contains the low-level entry code that runs before paging is fully initialized.</li> <li><code>.text</code>: The main executable code, starting at <code>0xC0000000</code> (virtual) but loaded at <code>0x00100000</code> (physical).</li> <li><code>.rodata</code>: Read-only constants and string literals.</li> <li><code>.data</code>: Initialized global and static variables.</li> <li><code>.bss</code>: Uninitialized variables (zeroed by the linker/loader) and the kernel stack.</li> <li><code>_kernel_end</code>: A symbol marking the end of the kernel image, used by the PMM to find free memory.</li> </ul>"},{"location":"kernel/kernel-init/#4-c-kernel-init-init_kernelc","title":"4. C Kernel Init (<code>init_kernel.c</code>)","text":"<p>The following subsystems are initialized in order:</p> <ol> <li>VGA/TTY: Clears the screen and prepares for text output.</li> <li>GDT: Replaces the GRUB-provided table with our own (Code, Data, and TSS).</li> <li>IDT: Configures interrupt gates and remaps the PIC.</li> <li>Keyboard: Sets up the PS/2 keyboard driver.</li> <li>Memory:</li> <li>Calculates the end of the kernel image and any loaded modules.</li> <li>Initializes the Physical Memory Manager (PMM) and Paging.</li> <li>Identity mapping is removed.</li> <li>Kernel Heap: Initializes <code>kmalloc</code> with a 1MB pool.</li> <li>Scheduler: Initializes the multitasking system and creates the first \"Main\" task.</li> </ol>"},{"location":"kernel/kernel-init/#4-post-initialization","title":"4. Post-Initialization","text":"<p>Once <code>init_kernel</code> returns, the kernel enters its main execution loop (typically in <code>kernel.c</code>), where it can start user tasks or run system services.</p>"},{"location":"kernel/kernel-utilities/","title":"Kernel Utility Functions","text":"<p>TilekarOS provides a set of low-level utilities to interact with the CPU and hardware.</p>"},{"location":"kernel/kernel-utilities/#1-io-port-access","title":"1. I/O Port Access","text":"<p>These functions are used to communicate with hardware controllers (like the PIC, PIT, and Keyboard).</p> <ul> <li><code>out_port_b(port, value)</code>: Sends a byte to an I/O port.</li> <li><code>in_port_b(port)</code>: Reads a byte from an I/O port.</li> </ul>"},{"location":"kernel/kernel-utilities/#2-interrupt-control","title":"2. Interrupt Control","text":"<p>Atomic operations are implemented using these functions to prevent race conditions during sensitive kernel sections.</p> <pre><code>/**\n * interrupt_save - Saves the current EFLAGS and disables interrupts (cli).\n * Return: The saved EFLAGS value.\n */\nuint32_t flags = interrupt_save();\n\n// --- CRITICAL SECTION ---\n\n/**\n * interrupt_restore - Restores the EFLAGS (and thus interrupt state).\n */\ninterrupt_restore(flags);\n</code></pre>"},{"location":"kernel/kernel-utilities/#implementation-details","title":"Implementation Details:","text":"<ul> <li><code>interrupt_save</code>: Uses <code>pushfl</code> to push the EFLAGS register onto the stack, <code>popl</code> to store it in a C variable, and then executes <code>cli</code>.</li> <li><code>interrupt_restore</code>: Uses <code>pushl</code> to push the saved EFLAGS back onto the stack and <code>popfl</code> to restore the CPU state.</li> </ul>"},{"location":"kernel/kernel-utilities/#3-register-context-interuptreg","title":"3. Register Context (<code>InteruptReg</code>)","text":"<p>When an interrupt or exception occurs, the CPU and assembly stubs save a complete snapshot of the registers on the stack.</p>"},{"location":"kernel/kernel-utilities/#important-fields","title":"Important Fields:","text":"<ul> <li><code>gs</code>, <code>fs</code>, <code>es</code>, <code>ds</code>: Pushed by segment registers.</li> <li><code>edi</code>, <code>esi</code>, <code>ebp</code>, <code>esp</code>, <code>ebx</code>, <code>edx</code>, <code>ecx</code>, <code>eax</code>: Pushed by <code>pushad</code>.</li> <li><code>intr_num</code>: The interrupt number being handled (0-255).</li> <li><code>err_cod</code>: Optional error code from the CPU (e.g., from Page Fault).</li> <li><code>eip</code>, <code>csm</code>, <code>eflags</code>: Current instruction pointer and CPU state.</li> <li><code>useresp</code>, <code>ss</code>: Only present if the interrupt occurred in User Mode.</li> </ul>"},{"location":"kernel/kernel-utilities/#4-helper-macros","title":"4. Helper Macros","text":"<ul> <li><code>CEIL_DIV(a, b)</code>: Calculates <code>(a + b - 1) / b</code>. This is commonly used in memory management for calculating the number of pages required for a given size.</li> </ul>"},{"location":"kernel/memory-management/","title":"Memory Management Internals","text":"<p>TilekarOS uses a tiered memory management system, transitioning from raw physical memory allocation to virtual memory with recursive paging.</p>"},{"location":"kernel/memory-management/#1-physical-memory-manager-pmm","title":"1. Physical Memory Manager (PMM)","text":"<p>The PMM tracks 4KB page frames in physical RAM using a bitmap.</p> <ul> <li>Allocation: <code>pmm_alloc_page_frame()</code> finds the first free bit and returns the physical address.</li> <li>Deallocation: <code>pmm_free_page_frame(phys_addr)</code> clears the bit.</li> </ul>"},{"location":"kernel/memory-management/#2-virtual-memory-manager-vmm-paging","title":"2. Virtual Memory Manager (VMM) &amp; Paging","text":"<p>TilekarOS implements 32-bit Paging with a Flat Memory Model.</p>"},{"location":"kernel/memory-management/#memory-map","title":"Memory Map","text":"<ul> <li>0x00000000 - 0xBFFFFFFF: User Space (3GB)</li> <li>0xC0000000 - 0xFFFFFFFF: Kernel Space (1GB, Higher-Half)</li> </ul>"},{"location":"kernel/memory-management/#recursive-paging","title":"Recursive Paging","text":"<p>To modify page directories/tables while paging is enabled, we map the Page Directory into itself at the last entry (1023).</p> <ul> <li>PD access: 0xFFFFF000</li> <li>PT access: 0xFFC00000 - 0xFFFFEFFF</li> </ul>"},{"location":"kernel/memory-management/#3-kernel-heap-kmalloc","title":"3. Kernel Heap (kmalloc)","text":"<p>TilekarOS provides a standard allocation interface using a doubly linked list of blocks.</p>"},{"location":"kernel/memory-management/#initialization-kmalloc_init","title":"Initialization (<code>kmalloc_init</code>)","text":"<p>The heap is initialized during kernel startup with a call to <code>kmalloc_init(initial_size)</code>. It sets the base of the heap at the <code>KERNEL_MALLOC</code> virtual address (defined in <code>local_config.h</code>).</p>"},{"location":"kernel/memory-management/#allocation-strategy","title":"Allocation Strategy","text":"<ul> <li>First-Fit: The allocator searches from the <code>head</code> for the first free block large enough for the request.</li> <li>8-Byte Alignment: All allocations are automatically aligned to 8-byte boundaries to satisfy CPU requirements and improve performance.</li> <li>Block Splitting: If a block is significantly larger than requested, it is split into two to reduce internal fragmentation.</li> <li>Heap Extension (<code>heap_sbrk</code>): If no suitable block is found, <code>heap_sbrk</code> is called. This function:<ol> <li>Calculates how many new 4KB physical pages are needed.</li> <li>Allocates them via the PMM.</li> <li>Maps them into the virtual address space using the VMM.</li> <li>Extends the linked list of blocks.</li> </ol> </li> </ul>"},{"location":"kernel/memory-management/#deallocation-kfree","title":"Deallocation (<code>kfree</code>)","text":"<ul> <li>Coalescing: When a block is freed, the allocator automatically merges it with any adjacent free blocks (previous or next) to combat external fragmentation.</li> </ul>"},{"location":"kernel/memory-management/#thread-safety","title":"Thread Safety","text":"<p>Both <code>kmalloc</code> and <code>kfree</code> are wrapped in <code>interrupt_save()</code> and <code>interrupt_restore()</code> calls. This ensures that memory allocation is atomic and safe even if an interrupt occurs (e.g., a timer interrupt triggering a context switch) during the process.</p>"},{"location":"kernel/memory-management/#api","title":"API","text":"<ul> <li><code>kmalloc(size)</code>: Allocates <code>size</code> bytes. Returns a pointer to the payload.</li> <li><code>kfree(ptr)</code>: Frees the block starting at <code>ptr</code>.</li> <li><code>kcalloc(n, size)</code>: Allocates <code>n * size</code> bytes and clears the memory to zero.</li> <li><code>krealloc(ptr, size)</code>: Resizes an allocation. If the new size is smaller, it may return the same pointer. If larger, it allocates a new block and copies the data.</li> </ul>"},{"location":"kernel/multitasking/","title":"Multitasking &amp; Scheduling","text":"<p>TilekarOS supports preemptive multitasking for Ring 0 (Kernel) and Ring 3 (User) tasks.</p>"},{"location":"kernel/multitasking/#1-task-structure-task_t","title":"1. Task Structure (<code>task_t</code>)","text":"<p>A task tracks:</p> <ul> <li>ID: Unique process identifier (TID).</li> <li>State: READY, RUNNING, ZOMBIE.</li> <li>Kernel Stack: Pointer to the current stack pointer of the task.</li> <li>Address Space: Page Directory physical address (<code>CR3</code>).</li> <li>Privilege: Kernel (0) or User (3).</li> </ul>"},{"location":"kernel/multitasking/#2-task-lifecycle","title":"2. Task Lifecycle","text":""},{"location":"kernel/multitasking/#creation-task_create","title":"Creation (<code>task_create</code>)","text":"<p>When a task is created, the kernel:</p> <ol> <li>Allocates a <code>task_t</code> and a 4KB kernel stack.</li> <li>Prepares the stack frame to simulate an interrupt return (<code>iret</code>).</li> <li>Initializes registers so the task starts execution at <code>task_wrapper</code>.</li> </ol>"},{"location":"kernel/multitasking/#execution-wrapper-task_wrapper","title":"Execution Wrapper (<code>task_wrapper</code>)","text":"<p>Every task begins in <code>task_wrapper</code>.</p> <ul> <li>For User Tasks, it handles the transition to Ring 3 using <code>iret</code>.</li> <li>For Kernel Tasks, it simply calls the entry point.</li> <li>When the task finishes, it automatically calls <code>task_exit()</code>.</li> </ul>"},{"location":"kernel/multitasking/#exiting-task_exit","title":"Exiting (<code>task_exit</code>)","text":"<p>When a task exits, it is removed from the ready queue and placed into a Zombie List.</p> <ul> <li>Zombie List: Exited tasks cannot free their own kernel stack while they are still using it.</li> <li>Delayed Cleanup: The next task to be scheduled calls <code>cleanup_zombies()</code>, which performs the actual <code>kfree</code> on the exited task's <code>task_t</code> and kernel stack.</li> <li>System Halt: If the last remaining task exits, the kernel enters an infinite <code>hlt</code> loop.</li> </ul>"},{"location":"kernel/multitasking/#3-context-switching","title":"3. Context Switching","text":"<p>A context switch occurs during a timer interrupt (preemption) or a manual <code>task_yield()</code>.</p>"},{"location":"kernel/multitasking/#context_switchcurrent_esp-next_esp-next_cr3-intr_num","title":"<code>context_switch(current_esp, next_esp, next_cr3, intr_num)</code>","text":"<p>The switch is performed in assembly (<code>task.asm</code>) and is highly optimized:</p> <ol> <li>Simulate Interrupt: Since <code>context_switch</code> is a regular call, it pops the return EIP and pushes a \"fake\" interrupt frame (<code>EFLAGS</code>, <code>CS</code>, <code>EIP</code>) so that it can return via <code>iret</code>.</li> <li>Save Current Context: Saves all registers (<code>pushad</code>, segment registers) onto the current task's stack.</li> <li>Page Directory Switch: Checks if the <code>next_cr3</code> is different from the current one. If so, it loads the new <code>CR3</code>. This avoids unnecessary TLB flushes if switching between threads in the same process.</li> <li>Stack Switch: Updates the current task's <code>task_t</code> with its current <code>ESP</code> and then loads the <code>next_esp</code> into the <code>ESP</code> register.</li> <li>PIC EOI: If the switch was triggered by a hardware interrupt (<code>intr_num &gt;= 32</code>), it calls <code>pic_send_eoi</code> before restoring the next task's state.</li> <li>Restore Context: Pops the segment and general-purpose registers of the new task.</li> <li>Return: Executes <code>iret</code>, which jumps to the next task's saved <code>EIP</code> and restores its <code>EFLAGS</code>.</li> </ol>"},{"location":"kernel/multitasking/#4-user-mode-ring-3","title":"4. User Mode (Ring 3)","text":"<p>The kernel transitions to User Mode using the <code>iret</code> instruction within <code>task_wrapper</code> (for new tasks) or when returning from a syscall/interrupt.</p>"},{"location":"kernel/multitasking/#ring-3-transition-logic","title":"Ring 3 Transition Logic:","text":"<ul> <li>TSS: The global Task State Segment is updated with the task's <code>esp0</code> (top of its kernel stack) during every context switch. This allows the CPU to find the kernel stack when an interrupt occurs in Ring 3.</li> <li>User Stack: For user tasks, a separate stack is mapped at <code>0xB0000000</code> in their private address space.</li> <li>The Jump:<ol> <li>Sets segment registers (<code>DS</code>, <code>ES</code>, <code>FS</code>, <code>GS</code>) to the user data selector (<code>0x23</code>).</li> <li>Pushes the User Stack Segment (<code>0x23</code>), the User Stack Pointer, <code>EFLAGS</code> (with interrupts enabled), the User Code Segment (<code>0x1B</code>), and the entry point <code>EIP</code>.</li> <li>Executes <code>iret</code>.</li> </ol> </li> </ul>"},{"location":"kernel/syscalls/","title":"System Call Internals","text":"<p>System calls are the primary interface between user space and the kernel. In TilekarOS, we use the <code>int 0x80</code> software interrupt to transition to kernel mode.</p>"},{"location":"kernel/syscalls/#1-syscall-dispatcher","title":"1. Syscall Dispatcher","text":"<p>The dispatcher is responsible for taking the registers from an <code>int 0x80</code> interrupt and calling the corresponding C function.</p>"},{"location":"kernel/syscalls/#register-mapping","title":"Register Mapping","text":"<p>When <code>int 0x80</code> is called:</p> <ul> <li>EAX: Syscall Number</li> <li>EBX: 1st Argument</li> <li>ECX: 2nd Argument</li> <li>EDX: 3rd Argument</li> <li>ESI: 4th Argument</li> <li>EDI: 5th Argument</li> </ul> <p>The return value of the syscall is placed back into EAX.</p>"},{"location":"kernel/syscalls/#assembly-stub-syscall_stub","title":"Assembly Stub (<code>syscall_stub</code>)","text":"<p>The <code>syscall_stub</code> in <code>syscall.asm</code> handles the low-level transition:</p> <ol> <li>Context Save: Pushes a dummy error code, the interrupt number (0x80), all general-purpose registers (<code>pushad</code>), and segment registers.</li> <li>Kernel Data Setup: Sets <code>DS</code> and <code>ES</code> to the kernel data selector (<code>0x10</code>).</li> <li>Call Dispatch: Passes the current stack pointer (<code>ESP</code>) as a pointer to <code>InteruptReg</code> to the C handler <code>syscall_handler</code>.</li> <li>Restore State: After the handler returns, it pops the saved context and executes <code>iretd</code> to return to user mode.</li> </ol>"},{"location":"kernel/syscalls/#syscall-table","title":"Syscall Table","text":"<p>The <code>syscall_dispatch</code> function uses the <code>EAX</code> register as an index into the <code>syscall_table</code>. If the index is valid, it calls the corresponding handler: <pre><code>uint32_t syscall_dispatch(uint32_t num, uint32_t a, uint32_t b, uint32_t c, uint32_t d, uint32_t e) {\n    if (num &gt;= SYS_MAX) return -1;\n    return syscall_table[num](a, b, c, d, e);\n}\n</code></pre></p>"},{"location":"kernel/syscalls/#2-kernel-user-interaction","title":"2. Kernel-User Interaction","text":"<p>Syscalls are handled on the kernel stack of the calling task.</p> <ul> <li>The kernel can safely access user-space memory (because it is mapped in the lower 3GB), but care must be taken to validate pointers to prevent security issues.</li> <li>If a syscall blocks (e.g., waiting for I/O), the task's state is set to <code>BLOCKED</code> and the scheduler picks another task.</li> </ul>"},{"location":"kernel/tasks-guide/","title":"Task Management","text":"<p>TilekarOS supports preemptive multitasking for both kernel and user-mode tasks.</p>"},{"location":"kernel/tasks-guide/#1-creating-a-kernel-task","title":"1. Creating a Kernel Task","text":"<p>Kernel tasks run with full privileges (Ring 0) and share the kernel's address space.</p> <pre><code>/**\n * task_create - Creates a new kernel task.\n * @entry: The entry point function for the task.\n * @privilege_level: 0 for kernel.\n */\ntask_t* task = task_create(my_kernel_function, 0);\n</code></pre>"},{"location":"kernel/tasks-guide/#what-happens","title":"What happens:","text":"<ul> <li>A unique Task ID (TID) is assigned.</li> <li>A 4KB kernel stack is allocated.</li> <li>The task is added to the circular ready queue.</li> <li>It will begin execution from <code>my_kernel_function</code> when scheduled.</li> </ul>"},{"location":"kernel/tasks-guide/#2-creating-a-user-task","title":"2. Creating a User Task","text":"<p>User tasks run with restricted privileges (Ring 3) in their own isolated virtual address space.</p>"},{"location":"kernel/tasks-guide/#why-user-tasks","title":"Why User Tasks?","text":"<p>In TilekarOS, user tasks provide Fault Isolation and Security:</p> <ul> <li>Protection: A bug in a user task (like a null pointer dereference) will only crash that task, not the whole kernel.</li> <li>Controlled Access: User tasks must go through a formal System Call interface to perform privileged actions like writing to the terminal or managing files.</li> <li>Address Space Isolation: Each user task gets its own Page Directory, meaning <code>0x08048000</code> points to different physical memory for every process.</li> </ul>"},{"location":"kernel/tasks-guide/#example-defining-and-launching-a-user-task","title":"Example: Defining and Launching a User Task","text":"<ol> <li> <p>Define the code (typically in an assembly file):     <pre><code>_start_user_task:\n    ; syscall: sys_write(1, msg, 19)\n    mov eax, 1\n    mov ebx, 1\n    mov ecx, msg\n    mov edx, 19\n    int 0x80\n\n    ; syscall: sys_exit(0)\n    mov eax, 0\n    mov ebx, 0\n    int 0x80\n\nmsg db 'Hello from Ring 3!', 0xA, 0\n_end_user_task:\n</code></pre></p> </li> <li> <p>Launch from the kernel:     <pre><code>extern char _start_user_task;\nextern char _end_user_task;\n\ntask_t* user_task = task_create_user(&amp;_start_user_task, &amp;_end_user_task);\n</code></pre></p> </li> </ol>"},{"location":"kernel/tasks-guide/#implementation-details","title":"Implementation Details:","text":"<ul> <li>Isolated Address Space: <code>task_create_user</code> creates a new page directory for the task.</li> <li>Memory Mapping:</li> <li>The user's code is mapped at <code>0x08048000</code> in its virtual address space.</li> <li>A 4KB user stack is mapped at <code>0xB0000000</code>.</li> <li>Privilege Transition: The kernel prepares a stack frame and uses the <code>iret</code> instruction to jump to the code at <code>0x08048000</code> while dropping the CPU to Ring 3.</li> <li>Protection: User tasks cannot execute privileged instructions (like <code>hlt</code>) or access kernel memory (<code>&gt; 0xC0000000</code>). They must use the <code>int 0x80</code> interface for all system services.</li> </ul>"},{"location":"kernel/tasks-guide/#3-task-lifecycle","title":"3. Task Lifecycle","text":"<ul> <li>Ready: Task is waiting for its turn in the scheduler.</li> <li>Running: Task currently owns the CPU.</li> <li>Exiting: When a task returns from its entry point, <code>task_exit()</code> is called automatically.</li> <li>Zombie: Exited tasks remain as \"zombies\" until the scheduler cleans up their allocated memory (stack and <code>task_t</code> structure).</li> </ul>"},{"location":"kernel/tasks-guide/#4-scheduling","title":"4. Scheduling","text":"<p>The scheduler uses a Round-Robin algorithm triggered by the Timer (PIT) every 10ms.</p> <ul> <li>Yielding: A task can voluntarily give up the CPU by calling <code>task_yield()</code>.</li> <li>Preemption: The timer interrupt forces a context switch if the current task's time slice has expired.</li> </ul>"}]}